import { FindDriverLeaveByIdType } from "@ryogo-travel-app/api/services/driver.services"

export default function ModifyDriverLeavePageComponent({
  leave,
}: {
  leave: FindDriverLeaveByIdType
}) {
  return <div></div>
}
